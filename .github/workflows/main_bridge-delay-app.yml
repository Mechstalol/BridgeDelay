name: Build & deploy – bridge-delay-app

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
# ────────────────────────────────
# 1️⃣  BUILD & PUSH IMAGE
# ────────────────────────────────
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
    - uses: actions/checkout@v4

    # login to ACR
    - name: Log in to ACR
      uses: docker/login-action@v3
      with:
        registry:  ${{ secrets.REGISTRY_LOGIN_SERVER }}
        username:  ${{ secrets.REGISTRY_USERNAME }}
        password:  ${{ secrets.REGISTRY_PASSWORD }}

    # build & push image
    - name: Build & push image
      uses: docker/build-push-action@v5
      with:
        context: .
        push:   true
        tags:   ${{ secrets.REGISTRY_LOGIN_SERVER }}/bridge-delay-app:latest
        # — flip “latest” to “${{ github.sha }}” any time you want unique tags

# ────────────────────────────────
# 2️⃣  DEPLOY TO AZURE WEB APP
# ────────────────────────────────
  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read

    steps:
    - name: Azure login (OIDC)
      uses: azure/login@v2
      with:
        client-id:       ${{ secrets.AZUREAPPSERVICE_CLIENTID_993C9A456F1E4089B56B5CF329D0BF1D }}
        tenant-id:       ${{ secrets.AZUREAPPSERVICE_TENANTID_290B337912884FBBBE4FE16062C5D5D8 }}
        subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_8438F652E74442D8A0478EB2E5D6168E }}


    - name: Deploy container to Web App
      uses: azure/webapps-deploy@v3
      with:
        app-name: bridge-delay-app
        slot-name: Production
        images:   ${{ secrets.REGISTRY_LOGIN_SERVER }}/bridge-delay-app:latest
